export const rectangle = (base: number, height: number): number => base * height;
/* 
    CommonJS syntax would be:
    export:
        module.export = {
            rectangle,
            ...
        }
*/